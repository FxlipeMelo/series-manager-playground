{% extends 'base.html.twig' %}

{% block title %}{{ series is defined ? 'Editar' : 'Nova' }}{% endblock %}
{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_widget(form.save, {'attr': {'class': 'btn btn-dark'}}) }}
    {{ form_end(form) }}
{% endblock %}
